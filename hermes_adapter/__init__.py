"""hermes-adapter — sidecar adapter for hermes-agent.

Provides an A2A (Agent-to-Agent) server and a workspace HTTP API
(filesystem, git, Sylang symbols) as a separate process that runs alongside
stock hermes-agent.
"""

__version__ = "0.1.0"
